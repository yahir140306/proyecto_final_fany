<!-- Navigation Bar -->
<nav
  class="sticky top-0 left-0 w-full z-50 bg-gray-900/90 backdrop-blur-lg shadow-lg border-b border-gray-700/50"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div
      class="flex flex-col md:flex-row justify-between items-center h-auto py-3 md:py-0"
    >
      <!-- Logos y Sección Izquierda -->
      <div class="flex items-center space-x-4 mb-3 md:mb-0">
        <a
          href="/"
          class="transform hover:scale-105 transition-transform duration-300 nav-logo"
        >
          <img
            src="/Images/logo_utsh.png"
            alt="UTSH"
            class="h-10 sm:h-12 w-auto rounded-lg shadow-md"
          />
        </a>
        <div
          class="h-6 sm:h-8 w-px bg-gradient-to-b from-purple-400 to-blue-400"
        >
        </div>
        <a
          href="/"
          class="transform hover:scale-105 transition-transform duration-300 nav-logo"
        >
          <img
            src="/Images/logo_sep.webp"
            alt="SEP"
            class="h-10 sm:h-12 w-auto rounded-lg shadow-md"
          />
        </a>
      </div>

      <!-- Navegación -->
      <div
        class="flex flex-wrap justify-center md:justify-end items-center gap-2 py-2 md:h-16"
      >
        <a href="/" class="nav-link">Inicio</a>
        <a href="/Introduccion" class="nav-link">Introducción</a>
        <a href="/Metodologia" class="nav-link">Metodología</a>
        <a href="/AnalisisDeDatos" class="nav-link">Análisis</a>
        <a href="/Conclusiones" class="nav-link">Conclusiones</a>
        <a href="/Recomendaciones" class="nav-link">Recomendaciones</a>
        <a href="/Referencias" class="nav-link">Referencias</a>
        <a href="/Apendices" class="nav-link">Apéndices</a>
      </div>
    </div>
  </div>
</nav>

<style>
  /* Estilos para los enlaces de navegación */
  .nav-link {
    position: relative;
    display: inline-block;
    padding: 0.5rem 0.75rem;
    color: #e0e7ff;
    font-weight: 500;
    font-size: 0.9rem;
    text-decoration: none;
    border-radius: 0.5rem;
    transition: all 0.3s ease;
    overflow: hidden;
    background-color: rgba(79, 70, 229, 0.1);
    backdrop-filter: blur(4px);
  }

  .nav-link:before {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, #8b5cf6, #3b82f6);
    transition: width 0.3s ease;
  }

  .nav-link:hover {
    color: white;
    background-color: rgba(79, 70, 229, 0.2);
    transform: translateY(-2px);
  }

  .nav-link:hover:before {
    width: 100%;
  }

  .nav-link:active {
    transform: translateY(0);
  }

  /* Navegación responsiva */
  @media (max-width: 768px) {
    .nav-link {
      padding: 0.4rem 0.6rem;
      font-size: 0.8rem;
      margin: 0.15rem;
    }
  }

  /* Mejoras para navegación táctil */
  .nav-logo {
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }

  .nav-logo:active {
    transform: scale(0.95);
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    // Agregar soporte táctil para los logos
    const logos = document.querySelectorAll(".nav-logo");
    logos.forEach((logo) => {
      const logoElement = logo as HTMLElement;

      logoElement.addEventListener(
        "touchstart",
        function () {
          logoElement.style.transform = "scale(0.95)";
        },
        { passive: true }
      );

      logoElement.addEventListener(
        "touchend",
        function () {
          logoElement.style.transform = "scale(1.05)";
          setTimeout(() => {
            logoElement.style.transform = "";
          }, 200);
        },
        { passive: true }
      );
    });

    // Agregar soporte táctil para los enlaces de navegación
    const navLinks = document.querySelectorAll(".nav-link");
    navLinks.forEach((link) => {
      const linkElement = link as HTMLElement;

      linkElement.addEventListener(
        "touchstart",
        function () {
          linkElement.style.backgroundColor = "rgba(79, 70, 229, 0.3)";
          linkElement.style.transform = "translateY(-1px)";
        },
        { passive: true }
      );

      linkElement.addEventListener(
        "touchend",
        function () {
          linkElement.style.backgroundColor = "";
          linkElement.style.transform = "";
        },
        { passive: true }
      );
    });
  });
</script>
